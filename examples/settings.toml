# WiFi credentials (also used when CIRCUITPY_WIFI_* below are not set)
wifi_ssid="YOUR WIFI SSID"
wifi_password="YOUR WIFI PASSWORD"
anonymous_greetings="Person A.,Person B."

# quotation marks matter!
MQTT_BROKER="broker-name-or-IP-address"
MQTT_PORT=1883
MQTT_SSL=0
MQTT_CLIENTID="optional Client ID"
MQTT_USERNAME="username"
MQTT_PASSWORD="password"
MQTT_TOPIC_PREFIX="givemeasign"

# Matrix Portal S3: these connect to WiFi at boot (alternative to wifi_ssid above)
CIRCUITPY_WIFI_SSID="not-so-secret-ssid"
CIRCUITPY_WIFI_PASSWORD="secretpassword"

# Optional CircuitPython web workflow (file editing over WiFi, not the sign API)
# Leave the User field blank in the browser.
CIRCUITPY_WEB_API_PASSWORD="passw0rd"
CIRCUITPY_WEB_API_PORT=80
CIRCUITPY_WEB_INSTANCE_NAME=""
